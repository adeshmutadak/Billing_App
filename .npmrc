# react-native-print@0.11.0 declares a stale peer dependency on
# react-native-windows, which pulls in a React version that conflicts with the
# pinned react@19.1.1. This project targets Android and iOS only, so the peer
# is not applicable. Required for npm install to resolve.
legacy-peer-deps=true
